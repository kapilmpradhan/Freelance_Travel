services:
  job-handler:
    image: $DOCKER_IMAGE_NAME:$VERSION
    container_name: freelance-travel-job-handler
    platform: linux/amd64
    entrypoint: /usr/local/bin/cron-setup.sh
    restart: always
    networks:
      - $NETWORK_NAME

networks:
  $NETWORK_NAME:
    external: true
